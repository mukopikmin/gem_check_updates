source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in gem_check_updates.gemspec
gemspec

gem 'simplecov', '~> 0.13.0'
